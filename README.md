# DOMINO Project

## Setup

1. Clone repository:
git clone https://github.com/weibi-weibi/DOMINO.git

2. Create environment:
python -m venv venv
source venv/Scripts/activate  # Windows
pip install -r requirements.txt

3. Run notebooks:
jupyter notebook
